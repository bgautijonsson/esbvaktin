# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/

# Environment variables
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data (large fetched files)
data/**/*.csv
data/**/*.json
data/**/*.parquet
!data/.gitkeep
!data/seeds/**
data/seeds/generated/

# Inbox (processed ephemeral data)
data/inbox/

# Analysis working directories
data/analyses/

# Export (generated, copied to site repo)
data/export/

# Pipeline working files (agent context/output, not source data)
data/reassessment/
data/evidence_is/
data/speech_checks/
data/overviews/
data/audit/
data/review/
data/heimildin/
data/backfill/
data/drift_fix/

# Scratch / one-off scripts (underscore-prefix convention).
# These are typically date-stamped generators kept locally for reproducibility
# but never committed. See ESB Knowledge/Operations/ for the convention.
scripts/_*

# Binary/personal files
esb_vaktin_kynning.docx
metill-ehf.skill

# Claude Code (keep skills, rules, agents, hooks, and shared settings)
.claude/*
!.claude/skills/
!.claude/rules/
!.claude/agents/
!.claude/hooks/
!.claude/settings.json
.claude/skills/**/.fuse_hidden*

# MCP config (user-local)
.mcp.json
data/althingi.db
althingi.db
.gstack/

# Local agent state (per-machine, not for source control)
.claire/
.superpowers/

# Personal project overrides (private, machine-local)
CLAUDE.local.md

# R / ad-hoc binary outputs
Rplots.pdf
data/esbvaktin.db
